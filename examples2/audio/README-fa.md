# Audio

این ملودی‌ها کوتاه و آموزشی هستند و برای این بسته به‌صورت original note exercises ساخته شده‌اند؛ هیچ قطعهٔ تجاری یا تنظیم وابسته به اثر دارای حق نشر استفاده نشده است. هر فایل با نت‌های فارسی `دو`، `ر`، `می`، `فا`، `سل`، `لا` و `سی` یک WAV پیوسته تولید می‌کند.
