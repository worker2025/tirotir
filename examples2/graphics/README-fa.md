# Graphics

این مثال‌ها با renderer فعلی، static SVG و live scene اجرا می‌شوند. مربع، مستطیل، مثلث، دایره، رنگ و ضخامت در مثال‌ها استفاده شده‌اند. animation زمان‌مند و event گرافیکی در نسخهٔ فعلی ادعا نمی‌شود.
